STRUCTURE: 
struct ListNode {
    int val;
    ListNode *next;
    ListNode() : val(0), next(nullptr) {}
    ListNode(int x) : val(x), next(nullptr) {}
    ListNode(int x, ListNode *next) : val(x), next(next) {}
};

ALGORITHMS: 
    * fast slow pointers 
    * cyclical hare and rabbit

SOLVING: 
    * iteration
    * recursion

METHODS: 
    * merge linked list 
    * reverse a linked list 
    * sort a linked list 